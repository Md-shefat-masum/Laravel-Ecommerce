<div class="">
    <input type="{{ $type }}" name="{{ $name }}" class="form-control"  placeholder="{{ $name }}" />
    <span class="text-danger {{ $name }}"></span>
    <style>
        .price{
            font-size: unset
        }
    </style>
</div>
